export default function Template({ children }: { children: React.ReactNode }) {
  return (
    // <div className="">
    <div className="backgroundDot min-h-screen">{children}</div>

    // </div>
  );
}
